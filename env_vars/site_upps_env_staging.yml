
# arteria-common
static_runfolder_path: "{{ static_proj_root }}/{{ uppmax_project }}/incoming/{{ deployment_environment }}"

# arteria archive upload
archive_upload_monitored_path: "{{ static_ngi_pipeline_nobackup }}/arteria/tmp"

# arteria delivery
project_path: "{{ static_runfolder_path }}/projects"
staging_path: "{{ static_runfolder_path }}/staged"
db_path: "{{ static_ngi_pipeline_path }}/db/arteria/{{ deployment_environment }}"
delivery_links: "{{ static_ngi_pipeline_nobackup }}/arteria/{{ deployment_environment }}/delivery_project_links"

# taca
snic_api_url: https://disposer.c3se.chalmers.se/supr-test/api

# tarzan
tarzan_host_name: miarka2.uppmax.uu.se
