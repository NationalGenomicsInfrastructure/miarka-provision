
# ngi_pipeline
ngi_pipeline_web_port: 6667

# arteria checksum
arteria_checksum_port: 10421
arteria_checksum_monitored_path: "{{ proj_root }}/{{ uppmax_project }}/incoming/{{ deployment_environment }}"

# arteria archive upload
archive_upload_port: 10481
archive_upload_monitored_path: "{{ ngi_pipeline_nobackup }}/arteria/tmp"

# arteria delivery
arteria_delivery_port: 10441
runfolder_path: "{{ proj_root }}/{{ uppmax_project }}/incoming/staging"
project_path: "{{ runfolder_path }}/projects"
staging_path: "{{ runfolder_path }}/staged"
db_path: "{{ ngi_pipeline_path }}/db/arteria/{{ deployment_environment }}"
