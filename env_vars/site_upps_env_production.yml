
# ngi_pipeline
ngi_pipeline_web_port: 6666

# arteria checksum
arteria_checksum_port: 10420
arteria_checksum_monitored_path: "{{ proj_root }}/{{ uppmax_project }}/incoming"

# arteria archive upload
archive_upload_port: 10480
archive_upload_monitored_path: "{{ ngi_pipeline_workdir }}/ANALYSIS/"

# arteria delivery
arteria_delivery_port: 10440
runfolder_path: "{{ proj_root }}/{{ uppmax_project }}/incoming"
project_path: "{{ ngi_pipeline_workdir }}/DELIVERY"
staging_path: "{{ ngi_pipeline_workdir }}/DELIVERY/arteria-staged"
db_path: "{{ ngi_pipeline_path }}/db/arteria"
